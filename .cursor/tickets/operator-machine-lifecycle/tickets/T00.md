# T00 — `packages/db/src/model-factory` (port from db-agent skill)

**Task ID:** T00  
**Agent:** db-agent  
**Depends on:** —  
**Parallel with:** —  
**Commit message:** `feat(db): model-factory utilities under packages/db`

---

## Description

| Deliverable | Detail |
|-------------|--------|
| **Location** | **`packages/db/src/model-factory/`** — **inside the database package**, not a separate workspace package. |
| **Factories** | Port from **`~/.cursor/plugins/agent-stack/src/skills/database/model-factory.md`**: `defineStaticEntity`, `defineVersionedEntity` → Drizzle `pgTable` (+ relations for versioned triple). |
| **Runtime** | Export `StaticTable`, `VersionedTable` wired to Drizzle and [`packages/db/src/transaction.ts`](../../../packages/db/src/transaction.ts) (`Transact` type). |
| **Types** | Export `Session`, `SYSTEM_KEYS`, create/update inputs omitting system-managed keys per skill. |
| **Exports** | Add **`packages/db/package.json`** entry e.g. `"./model-factory": { "default": "./src/model-factory/index.ts" }` so consumers can use **`@slushomat/db/model-factory`**. Optionally re-export selected symbols from [`packages/db/src/index.ts`](../../../packages/db/src/index.ts). |

**Naming:** Skill uses `organisationId` — Slushomat uses **`organizationId` / `organization_id`** and FKs to [`organization`](../../../packages/db/src/schema/auth.ts).

**Cycles:** Model-factory code must **only** depend on `drizzle-orm` / `pg-core` — **do not** import `packages/db/src/schema/*`. Schema files in T01 import the factory.

---

## Acceptance Criteria (traceable)

| AC ID | Criterion | This task delivers |
|-------|-----------|-------------------|
| Plan §1.1 | Factories colocated with DB | Module under `packages/db` |
| T01 | Unblocked | Schema can import factories |

---

## Context from Discovery

- [00-requirements.md](../00-requirements.md) — static `business_entity`, versioned `operator_contract` + changes audit.
- [03-plan.md](../03-plan.md) — §1.1; T00 → T01.

---

## Implementation Reference

- `~/.cursor/plugins/agent-stack/src/skills/database/model-factory.md`
- `~/.cursor/plugins/agent-stack/src/skills/database/_index.md`

---

## Files to Create / Modify

| Path | Change |
|------|--------|
| `packages/db/src/model-factory/**` | Implementation + `index.ts` barrel |
| `packages/db/package.json` | `exports["./model-factory"]` (explicit subpath) |
| `packages/db/src/index.ts` | Optional re-exports |

---

## Out of scope

- Feature schema tables (T01), API (T02+), UI (T05+).
