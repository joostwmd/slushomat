# T03 — tRPC admin: business entities, contracts, deployments

**Task ID:** T03  
**Agent:** api-agent  
**Depends on:** T02  
**Parallel with:** T04  
**Commit message:** `feat(api): admin machine lifecycle procedures`

---

## Description

| Router domain | Procedures (indicative) |
|---------------|-------------------------|
| **`admin.businessEntity`** | `listByOrganization`, `create`, `update`, `softDelete` — target **`organizationId`** from admin UI. |
| **`admin.operatorContract`** | `list` (filters), `get`, `create` (base + v1), `updateVersion`, `requestPdfUpload`, `confirmPdf`, `transitionStatus` — on **activate**, call **`assertAtMostOneActiveContractForMachine`**. |
| **`admin.machineDeployment`** | `start` (end prior open deployment, same tx), `end` (`endedAt`). |
| **Auth** | **`adminProcedure`** only. |
| **Validation** | `business_entity.organizationId === contract.organizationId`; machine exists. |
| **Revenue share** | Zod: **`revenueShareBasisPoints`** 0–10000. |
| **PDF** | Mirror [`admin-template-products.ts`](../../../apps/server/src/trpc/routers/admin-template-products.ts): signed URL → confirm → store on **version** row; MIME e.g. `application/pdf`. |

Merge into [`admin.ts`](../../../apps/server/src/trpc/routers/admin.ts) like existing sub-routers.

**Optional:** Use `VersionedTable` from **`@slushomat/db/model-factory`** in services for contract mutations if it speeds version/changes writes.

---

## Acceptance Criteria (traceable)

| AC ID | Criterion | This task delivers |
|-------|-----------|-------------------|
| FR-B | Admin contract lifecycle + PDF | Procedures |
| FR-A | Admin entity CRUD any org | `organizationId` on inputs |
| FR-C | Deployments | start/end |
| I-3, I-4, I-7, I-10 | Test surface | Exported procedures |

---

## Context from Discovery

- [00-requirements.md](../00-requirements.md) — FR-A–C, PDF, basis points.
- [01-ui-spec.md](../01-ui-spec.md) — admin flows, filters, transitions.

---

## Constraints

- **Transactions** for deployment start and contract activation.
- **No** operator mutations on contracts/deployments.

---

## Out of scope

- Operator routes (T04), admin UI (T06).
