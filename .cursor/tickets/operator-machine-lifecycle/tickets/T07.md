# T07 — Operator UI: businesses, contracts (read), fleet/slots

**Task ID:** T07  
**Agent:** frontend-agent  
**Depends on:** T04, T05  
**Parallel with:** —  
**Commit message:** `feat(operator): machine lifecycle and slots UI`

---

## Description

| Area | Detail |
|------|--------|
| **Routes** | `apps/operator-frontend/src/routes/_protected/$orgSlug/` — businesses, contracts (read-only), fleet/slots. |
| **Entities** | **T05** composite + **`operator.businessEntity.*`** with route **`orgSlug`**. |
| **Contracts** | Read-only, all statuses; PDF view/download (signed URL, read-only). |
| **Slots** | Product picker from **`operator.product.*`**; **left / middle / right**; if **no open deployment** — disabled + copy from [01-ui-spec.md](../01-ui-spec.md). |
| **Empty states** | No contract OK per spec. |
| **Nav** | Shell links. |

---

## Acceptance Criteria (traceable)

| AC ID | Criterion | This task delivers |
|-------|-----------|-------------------|
| FR-A, FR-B, FR-D, FR-E | Operator surfaces | Routes |
| 01-ui-spec | States & errors | Deployment / empty contract |

---

## Context from Discovery

- [01-ui-spec.md](../01-ui-spec.md) — Operator flows.
- [`$orgSlug/route.tsx`](../../../apps/operator-frontend/src/routes/_protected/$orgSlug/route.tsx) — org alignment.

---

## Constraints

- **No** admin-only mutations for contracts/deployments.
- Pass **`orgSlug`** on every lifecycle call.

---

## Out of scope

- Admin UI (T06), integration tests (T08).
