# T02 — Server helpers: org scope, entity guard, deployment, active contract

**Task ID:** T02  
**Agent:** api-agent  
**Depends on:** T01  
**Parallel with:** —  
**Commit message:** `feat(api): machine lifecycle guards and org helpers`

---

## Description

| Deliverable | Detail |
|-------------|--------|
| **Org resolution** | Reuse / extend [`getOrganizationIdForSlug`](../../../apps/server/src/lib/org-scope.ts), **`assertUserMemberOfOrg`**. Every **new** operator lifecycle procedure must resolve org from **`orgSlug`** and require **membership for that org** (see [03-plan.md](../03-plan.md) §1.1 vs current [`operatorProcedure`](../../../apps/server/src/trpc/procedures.ts)). |
| **`assertBusinessEntityBelongsToOrg`** | `(entityId, organizationId)` — reject if missing, wrong org, or soft-deleted per schema. |
| **`getOpenDeploymentForMachine`** | `(machineId)` → row with `endedAt == null` or null. |
| **`assertAtMostOneActiveContractForMachine`** | In **transaction** when activating: no other contract for same `machineId` has current version `status === 'active'` (optional `excludeContractBaseId`). |
| **Shared types (optional)** | `ContractStatus`, `MachineSlot` for Zod + UI. |

Artifact: extend [`org-scope.ts`](../../../apps/server/src/lib/org-scope.ts) and/or new `apps/server/src/lib/machine-lifecycle.ts`.

---

## Acceptance Criteria (traceable)

| AC ID | Criterion | This task delivers |
|-------|-----------|-------------------|
| FR-F | Membership + org primitives | Helpers documented for T03/T04 |
| Plan | Single active contract guard | Transaction-safe assertion |
| FR-D | Open deployment lookup | Query helper |

---

## Context from Discovery

- [00-requirements.md](../00-requirements.md) — FR-F, active contract rule, deployments.
- [02-test-spec.md](../02-test-spec.md) — I-1, I-4, I-7, I-9.

---

## Constraints

- Prefer **not** changing global `operatorProcedure` without product sign-off — per-router checks are safer.
- **No** new tRPC routes (T03/T04).

---

## Out of scope

- Routers (T03, T04), UI (T05–T07).
