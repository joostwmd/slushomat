# T06 — Review (security, storage lifecycle, consistency)

**Task ID:** T06  
**Agent:** reviewer-agent  
**Depends on:** T04  
**Parallel with:** —  
**Commit message:** `chore: review admin template products feature` *(optional — may be notes-only)*

---

## Description

Perform a **pre-merge** review of the full **admin-template-products** vertical: `@slushomat/supabase`, env split, Drizzle schema/migrations, admin tRPC router, and admin-frontend Products page + Dice UI upload.

Output: **short review notes** or a **PR checklist** (markdown in ticket thread, PR body, or `.cursor/tickets/admin-template-products/review-notes.md` — choose what the team prefers; **no code changes required** unless blocking issues are found).

---

## Acceptance Criteria (traceable)

Verify traceability to **all** requirements ACs (no IT-* — tests skipped per `02-test-spec.md`):

| AC ID | Review focus |
|-------|----------------|
| AC-1 | List shows correct fields; empty state; image/placeholder |
| AC-2 | Create persists and appears in list |
| AC-3 | Object in bucket; `template_product_image` row correct |
| AC-4 | Updates propagate |
| AC-5 | Delete removes DB rows; Storage policy honored; no orphaned objects if “delete storage” agreed |
| AC-6 | Non-admin cannot call procedures; UI only under admin route |
| AC-7 | No client-chosen paths; tokens short-lived; confirm validates prefix |
| AC-8 | Tax only 7/19 end-to-end |
| AC-9 | Orphan / retry UX sensible |
| AC-10 | Bad files rejected with clear feedback |

---

## Context from Discovery

### Plan (`03-plan.md`)

- **Human checkpoint 2:** Supabase bucket policies + env in deployment (server + Vite).
- Risks: path traversal, authz, storage errors, CORS, content types.

### Tests (`02-test-spec.md`)

- **Tests explicitly skipped** — review is **manual** + static analysis; do not request test suite additions as merge blocker unless team policy changes.

---

## Implementation Reference

### Checklist (suggested)

**Security**

- [ ] `adminProcedure` on **all** template product + upload procedures ([`apps/server/src/trpc/procedures.ts`](apps/server/src/trpc/procedures.ts)).
- [ ] `SUPABASE_SERVICE_ROLE_KEY` absent from client bundle (`grep` / Vite build analysis).
- [ ] Upload path prefix validated on **confirm**; no `..` or bucket escape.
- [ ] Content-type / size limits consistent server ↔ client.

**Data**

- [ ] Migrations apply on clean DB; FK `onDelete` matches delete story.
- [ ] Price + tax types consistent in schema, Zod, and UI formatting (cents vs decimal).

**Storage**

- [ ] Bucket name from env; documented in `.env.example`.
- [ ] Delete template product removes Storage object when policy requires it (T03).

**API / tRPC**

- [ ] Router mounted under [`apps/server/src/trpc/routers/admin.ts`](apps/server/src/trpc/routers/admin.ts); types flow to client.
- [ ] Errors map to usable messages (no raw stack traces to client).

**Frontend**

- [ ] Route + sidebar + breadcrumbs wired.
- [ ] Loading/error states; accessibility basics on file control (Dice UI + labels).
- [ ] Compare spacing/components to [`apps/admin-frontend/src/routes/_admin/machines.tsx`](apps/admin-frontend/src/routes/_admin/machines.tsx).

**Docs**

- [ ] Links still valid: [Supabase signed upload](https://supabase.com/docs/reference/javascript/storage-from-createsigneduploadurl), [Dice UI file upload](https://www.diceui.com/docs/components/file-upload).

---

## Files to Create / Modify

| Path | Change |
|------|--------|
| *(optional)* `.cursor/tickets/admin-template-products/review-notes.md` | Reviewer notes |
| Or PR description only | No repo file |

---

## Constraints

- **Scope:** This ticket is **review**, not a feature refactor. File follow-up tickets for non-blocking improvements.
- Align with **reviewer-agent** report style if the project has one.

---

## Skills

No repo-local reviewer skill file found. Use **reviewer-agent** standard pass: security first, then correctness, then UX polish.
