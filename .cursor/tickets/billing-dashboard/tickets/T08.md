# T08 — Review (optional)

**Task ID:** T08  
**Agent:** reviewer-agent  
**Depends on:** T05, T06  
**Parallel with:** T07  
**Commit message:** n/a (review produces a report, not a commit)

---

## Description

Final quality pass covering security, correctness, and maintainability.

| Area | Check |
|------|-------|
| **Security** | `operator.purchase.list` — no cross-org data leakage; `orgSlug` → org resolution + membership assert |
| **Security** | Machine server `POST /purchase` — only authenticated machines can create rows; `machineId` comes from auth context, never from request body |
| **Security** | `admin.*` — all customer + purchase procedures use `adminProcedure` |
| **Purchase attribution** | `organizationId` and `businessEntityId` correctly resolved at write time; `NO_ACTIVE_CONTRACT` guard fires correctly |
| **CSV export** | ZIP wrapping works; no sensitive server data leaked in client-side export |
| **Query performance** | Composite indexes on `purchase` are used by operator + admin list queries (check `EXPLAIN` or Drizzle query shape) |
| **Type safety** | No `any` casts outside the known model-factory limitation; `operatorContractVersion` cast isolated |
| **UI correctness** | "Configure Contract" query params correctly passed and read by contracts route |
| **Empty states** | All loading/empty states handled in both admin and operator UIs |

---

## Acceptance Criteria

- Report produced identifying any security gaps, performance issues, or missing error handling.
- All critical issues resolved before merge.

---

## Context from Discovery

- [00-requirements.md](../00-requirements.md) — FR-H (security)
- [03-plan.md](../03-plan.md) — §0 Human checkpoint 2; §T08

---

## Out of scope

- E2E test writing (T07 handles tests).
- PDF export review (out of scope for v1).
