# T07 — Integration tests (I-1 — I-10) + U-1

**Task ID:** T07  
**Agent:** test-writer-agent  
**Depends on:** T01, T02, T03  
**Parallel with:** T05, T06  
**Commit message:** `test(integration): billing-dashboard purchase and customer procedures`

---

## Description

| Deliverable | Detail |
|-------------|--------|
| **Integration tests** | I-1 through I-10 from `02-test-spec.md` — PGlite pattern per repo |
| **Unit test** | U-1 — CSV export utility (pure function; no DB needed) |
| **Machine server tests** | Use Hono test client (`hono/testing` or equivalent) |
| **tRPC tests** | Use standard repo `createCaller` / `createTestContext` pattern |
| **Time-box** | Document any skipped tests with explicit `test.skip(...)` + reason |

---

## Test coverage by area

### Machine server (Hono)

| Test | Spec ref |
|------|----------|
| Happy path: POST /purchase creates row with correct fields | I-1 |
| No active contract → 422 NO_ACTIVE_CONTRACT | I-2 |
| No open deployment → row with null businessEntityId | I-3 |
| Unauthenticated → 401 | (implicit auth guard) |

Setup for machine server tests:
- Seed: `machine`, `apikey`, `organization`, `operator_product`, `business_entity`.
- For I-1: also seed `operator_contract` (base + active version), `machine_deployment` (open).
- Use `db` from `@slushomat/db` against PGlite instance; swap via dependency injection or test env.

### tRPC — operator.purchase.list

| Test | Spec ref |
|------|----------|
| Returns only own-org purchases | I-4 |
| Cross-org → FORBIDDEN | I-5 |
| Date-range filter returns correct rows | I-6 |
| Entity filter returns correct rows | I-7 |

### tRPC — admin.purchase.list

| Test | Spec ref |
|------|----------|
| Admin can list any org's purchases | I-8 |
| machineId filter | I-10 |

### tRPC — admin.customer.list

| Test | Spec ref |
|------|----------|
| machineCount = active contracts only | I-9 |

### Unit — CSV export

| Test | Spec ref |
|------|----------|
| Output has correct header | U-1 |
| Amount is decimal (3.50), not cents (350) | U-1 |
| Date format YYYY-MM-DD | U-1 |

---

## Acceptance Criteria

| AC ID | Criterion |
|-------|-----------|
| I-1 – I-10 | Tests pass or are explicitly skipped with documented reason |
| U-1 | CSV utility test passes |

---

## Context from Discovery

- [02-test-spec.md](../02-test-spec.md) — full test table
- [03-plan.md](../03-plan.md) — §T07
- Existing integration test pattern: `packages/db` PGlite + `createUser → saveUser → getAuthHeaders → call route → assert → cleanup`

---

## Files to Create / Modify

| Path | Change |
|------|--------|
| `apps/server/src/trpc/routers/__tests__/admin-purchase.test.ts` (or similar) | New |
| `apps/server/src/trpc/routers/__tests__/operator-purchase.test.ts` | New |
| `apps/server/src/trpc/routers/__tests__/admin-customer.test.ts` | New |
| `apps/machine-server/src/__tests__/purchase.test.ts` | New |
| `packages/ui/src/composite/__tests__/purchases-export.test.ts` | New (U-1) |

---

## Out of scope

- E2E Playwright tests (optional; see 02-test-spec.md E-1, E-2).
- Concurrent insert race conditions.
