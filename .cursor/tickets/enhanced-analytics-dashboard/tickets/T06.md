# T06 — Review: security, performance, UX (optional)

**Task ID:** T06  
**Agent:** reviewer-agent  
**Depends on:** T03, T04, T05 *(T05 deferred — review completed without automated MV refresh)*  
**Parallel with:** —  
**Commit message:** *No code commit required — review-only; optional `docs:` or issue notes if findings need tracking*

**Deliverable:** [T06-findings.md](./T06-findings.md) (2026-03-23)

---

## Description

| Area | Review focus |
|------|----------------|
| **Security** | **SEC-001–003**: operator org isolation; admin platform vs scoped routes; no cross-org leakage; machine-only APIs reject forbidden filters (T01). Verify tRPC middleware / procedure bases (`operatorProcedure`, `adminProcedure`). |
| **Performance** | **`EXPLAIN (ANALYZE, BUFFERS)`** on hot paths: MV-backed historical reads, live “today” leg, admin platform aggregates. Confirm indexes from T00/T01 are used; note budget from [00-requirements.md](../00-requirements.md) Constraints (2s / 5s). |
| **UX / a11y** | Compare implemented UI to [01-ui-spec.md](../01-ui-spec.md): responsive grids, skeletons, per-chart errors, **hover tooltips**, keyboard/calendar behavior, **charts above tables**, **no chart → table filtering**, platform charts **only** on `/dashboard`. |
| **Consistency** | Berlin calendar periods; zeros for future days; MV ∪ today without double-count (spot-check with known data). |

### Output

- Short written review (PR comment, issue, or `tickets/T06-findings.md` only if team wants a paper trail — **do not** add unless requested).

---

## Acceptance Criteria

| AC ID | Criterion |
|-------|-----------|
| — | Review checklist completed; **P0/P1** issues filed or fixed before release. |
| SEC-001–003 | No unresolved isolation gaps. |
| FR-007 + UI spec | Tooltip-first UX; tables not driven by chart selection. |

---

## Context from Discovery

- [00-requirements.md](../00-requirements.md) — Security, performance constraints, FR-007.
- [01-ui-spec.md](../01-ui-spec.md) — Full UX/a11y target.
- [03-plan.md](../03-plan.md) — Human checkpoints §0, §T06.
- [02-test-spec.md](../02-test-spec.md) — Automated tests waived; manual validation per plan.

---

## Files to Create / Modify

| Path | Change |
|------|--------|
| *None required* | Review-only |

---

## Out of scope

- Implementing fixes (hand back to db/api/frontend agents as separate tasks).
- Writing automated tests (waived).
