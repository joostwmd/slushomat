# T05 — Ops: MV refresh schedule + runbook

**Task ID:** T05  
**Agent:** shell / devops  
**Depends on:** T00  
**Parallel with:** T01, T02  
**Commit message:** `chore(ops): analytics MV refresh schedule`

**Status (2026-03):** **Deferred** — implement later as a **Supabase Edge Function** (scheduled invoke) that runs `REFRESH MATERIALIZED VIEW CONCURRENTLY …` (or calls a DB RPC). Skip cron/script in-repo until that path is chosen.

---

## Description

| Deliverable | Detail |
|-------------|--------|
| **Command** | Document and automate **`REFRESH MATERIALIZED VIEW CONCURRENTLY <view_name>`** using the exact view name from T00. |
| **Schedule** | Run **after midnight Europe/Berlin** (e.g. **00:10 Europe/Berlin**) so “yesterday” is closed in the MV before operators rely on it. Align with how this project runs jobs (Supabase cron, GitHub Action, `apps/server/scripts`, worker, etc.). |
| **Artifact** | Checked-in **script** and/or **config** plus **`RUNBOOK.md`** (place under `.cursor/tickets/enhanced-analytics-dashboard/tickets/RUNBOOK.md` or `docs/` — pick one and link from runbook). |
| **Failure behavior** | Runbook notes: **log on failure**, where logs live, and who reacts; reference [00-requirements.md](../00-requirements.md) Failure State 1 / admin notification expectations if infra supports it. |
| **Idempotency** | Safe to re-run refresh; document lock/concurrency if relevant. |

### Product rules (echo)

- Refresh timing must match **Berlin** “day” semantics used in T00/T01.

---

## Acceptance Criteria

| AC ID | Criterion |
|-------|-----------|
| FR-010 | Daily concurrent refresh is operational and documented. |
| Failure State 1 | Runbook describes failure visibility + fallback expectations (coordinate with T01 if banner/logging added). |

---

## Context from Discovery

- [03-plan.md](../03-plan.md) — §T05, §1.1 (scheduling outside Postgres unless pg_cron).
- [00-requirements.md](../00-requirements.md) — FR-010, Failure State 1, Notes on monitoring.
- [02-test-spec.md](../02-test-spec.md) — Tests waived.

---

## Files to Create / Modify

| Path | Change |
|------|--------|
| e.g. `apps/server/scripts/refresh-analytics-mv.ts` or shell script | Invokes refresh (SQL via `psql`, Drizzle, or Supabase client per project norms) |
| CI / infra config | If project uses scheduled jobs, add job definition |
| `.cursor/tickets/enhanced-analytics-dashboard/tickets/RUNBOOK.md` or `docs/analytics-mv-runbook.md` | Schedule, command, failure checklist |

---

## Out of scope

- Defining MV SQL (T00).
- Application code for analytics queries (T01).
