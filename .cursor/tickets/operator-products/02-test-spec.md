# Test specification: Operator products (`operator-products`)

**Status: out of scope.** No unit, integration, or E2E tests are required for this feature. Validate manually against `00-requirements.md`.

(This file remains so discovery preconditions that expect `02-test-spec.md` still apply.)
