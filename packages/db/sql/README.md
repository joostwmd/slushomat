# Standalone SQL

Analytics MV DDL lives in a **Drizzle migration** (not here):

- `packages/db/src/migrations/0000_analytics_purchase_daily_summary.sql` → `pnpm db:migrate` from `packages/db`

See `../docs/analytics-mv.md`.
